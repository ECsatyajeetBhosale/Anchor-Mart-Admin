/**
 * test/setup.ts
 *
 * This file runs once before every test file.
 * It imports the jest-dom matchers so we can write assertions like:
 *   expect(element).toBeInTheDocument()
 *   expect(button).toBeDisabled()
 */

import "@testing-library/jest-dom";
